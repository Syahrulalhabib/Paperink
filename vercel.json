{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "cd paperink-react && npm install",
  "buildCommand": "cd paperink-react && npm run build",
  "outputDirectory": "paperink-react/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
